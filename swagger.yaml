openapi: 3.0.3
info:
  title: CargoFlow Logistics API
  version: 1.0.0
  description: API documentation for Shipment Management System

servers:
  - url: http://localhost:8000

paths:

  /health:
    get:
      summary: Health Check
      responses:
        "200":
          description: API working
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string

  # ---------------- AUTH ----------------

  /api/auth/register:
    post:
      summary: Register User
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRegister'
      responses:
        "201":
          description: User registered
        "400":
          description: Email already registered
        "422":
          description: Validation error

  /api/auth/login:
    post:
      summary: Login User
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserLogin'
      responses:
        "200":
          description: Login success
        "401":
          description: Invalid credentials
        "403":
          description: Account blocked

  /api/auth/me:
    get:
      summary: Get current user
      security:
        - bearerAuth: []
      responses:
        "200":
          description: User details

    patch:
      summary: Update user profile
      security:
        - bearerAuth: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserUpdate'
      responses:
        "200":
          description: Profile updated

  # ---------------- ADMIN ----------------

  /api/v1/admin/dashboard:
    get:
      summary: Admin dashboard
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Dashboard data

  /api/v1/admin/dashboard/shipments:
    get:
      summary: Admin shipments dashboard
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Shipment statistics

  /api/v1/admin/dashboard/agents:
    get:
      summary: Admin agents dashboard
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Agents data

  /api/v1/admin/dashboard/clients:
    get:
      summary: Admin clients dashboard
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Clients data

  /api/v1/admin/shipments:
    post:
      summary: Create shipment (Admin)
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AdminShipmentCreate'
      responses:
        "201":
          description: Shipment created
        "400":
          description: Invalid data
        "404":
          description: Client not found

  /api/v1/admin/shipments/{shipment_id}/assign/{agent_id}:
    post:
      summary: Assign agent to shipment
      security:
        - bearerAuth: []
      parameters:
        - name: shipment_id
          in: path
          required: true
          schema:
            type: integer
        - name: agent_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Agent assigned successfully
        "404":
          description: Shipment or agent not found

  /api/v1/admin/delivery_agents:
    post:
      summary: Add delivery agent
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeliveryAgentCreate'
      responses:
        "201":
          description: Delivery agent created
        "400":
          description: Email already exists

  /api/v1/admin/delivery_agents/{agent_id}/status:
    patch:
      summary: Block or unblock delivery agent
      security:
        - bearerAuth: []
      parameters:
        - name: agent_id
          in: path
          required: true
          schema:
            type: integer
      responses:
        "200":
          description: Status updated
        "404":
          description: Agent not found

  /api/v1/admin/business_clients:
    post:
      summary: Add business client
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UserRegister'
      responses:
        "201":
          description: Business client created
        "400":
          description: Email already exists

  /api/v1/admin/agents/live-location:
    get:
      summary: Get live location of agents
      security:
        - bearerAuth: []
      responses:
        "200":
          description: List of agent locations

  # ---------------- CLIENT ----------------

  /api/v1/client/dashboard:
    get:
      summary: Client dashboard
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Dashboard data

  /api/v1/client/shipments:
    get:
      summary: Get client shipments
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Shipment list

    post:
      summary: Create shipment (Client)
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ClientShipmentCreate'
      responses:
        "201":
          description: Shipment created
        "400":
          description: Business required

  /api/v1/client/track/{tracking_id}:
    get:
      summary: Track shipment
      parameters:
        - name: tracking_id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Tracking details
        "404":
          description: Invalid tracking ID

  /api/v1/client/business:
    post:
      summary: Create business
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BusinessCreate'
      responses:
        "201":
          description: Business created
        "400":
          description: Already exists

    put:
      summary: Update business
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BusinessCreate'
      responses:
        "200":
          description: Business updated
        "404":
          description: Business not found

  # ---------------- AGENT ----------------

  /api/v1/agent/dashboard:
    get:
      summary: Agent dashboard
      security:
        - bearerAuth: []
      responses:
        "200":
          description: Dashboard data

  /api/v1/agent/update/live-location:
    post:
      summary: Update agent live location
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LocationUpdate'
      responses:
        "200":
          description: Location updated
        "403":
          description: Not assigned to shipment
        "404":
          description: Shipment not found

# ---------------- COMPONENTS ----------------

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:

    UserRegister:
      type: object
      required: [name, email, password, phone, city]
      properties:
        name: { type: string }
        email: { type: string }
        password: { type: string }
        phone: { type: string }
        city: { type: string }

    UserLogin:
      type: object
      required: [email, password]
      properties:
        email: { type: string }
        password: { type: string }

    UserUpdate:
      type: object
      properties:
        name: { type: string }
        email: { type: string }
        phone: { type: string }
        password: { type: string }

    DeliveryAgentCreate:
      type: object
      required: [name, email, password, phone, city]
      properties:
        name: { type: string }
        email: { type: string }
        password: { type: string }
        phone: { type: string }
        city: { type: string }

    AdminShipmentCreate:
      type: object
      properties:
        sender_id: { type: integer }
        receiver_name: { type: string }
        receiver_phone: { type: string }
        receiver_email: { type: string }
        pickup_line1: { type: string }
        pickup_city: { type: string }
        pickup_state: { type: string }
        pickup_pincode: { type: string }
        delivery_line1: { type: string }
        delivery_city: { type: string }
        delivery_state: { type: string }
        delivery_pincode: { type: string }
        weight: { type: number }
        price: { type: number }

    ClientShipmentCreate:
      allOf:
        - $ref: '#/components/schemas/AdminShipmentCreate'

    LocationUpdate:
      type: object
      required: [lat, lng]
      properties:
        lat: { type: number }
        lng: { type: number }
        shipment_id: { type: integer }

    BusinessCreate:
      type: object
      required: [name, type]
      properties:
        name: { type: string }
        type: { type: string }